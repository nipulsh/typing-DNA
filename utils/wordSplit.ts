export default function splitCharacters(str: string): string[] {
  const string = str;
  const characters: string[] = [];

  for (let i = 0; i < string.length; i++) {
    characters.push(string[i]);
  }
  return characters;
}
